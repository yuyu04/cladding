id: F-40327b
slug: persona-skill-md-cleanup
title: persona + skill md drift cleanup (v0.3.41)
status: done
modules:
  - src/agents/developer.md
  - src/agents/planner.md
  - src/agents/orchestrator.md
  - src/agents/reviewer.md
  - src/agents/observability.md
  - src/agents/README.md
  - skills/init/SKILL.md
  - skills/run/SKILL.md
  - skills/status/SKILL.md
  - skills/check/SKILL.md
  - skills/doctor/SKILL.md
  - skills/checkpoint/SKILL.md
  - skills/rollback/SKILL.md
  - skills/route/SKILL.md
depends_on:
  - F-bb15e6
acceptance_criteria:
  - id: AC-001
    ears: ubiquitous
    action: realign every persona canonical prompt under src/agents/ with v0.3.40 reality so a consumer project installing the cladding plugin receives accurate guidance
    response: 'src/agents/developer.md swaps the docs/code-style.md reference for the consumer-facing docs/conventions.md auto-generated by clad init --scan with a fallback note for cladding-self; src/agents/planner.md replaces the F-NNN-only language with explicit F-<hash6> guidance for new features and adds a Scenarios policy paragraph documenting the v0.3.30 auto-extraction deprecation; src/agents/orchestrator.md adds an LLM-dispatcher-health row to the routing table that points at observability via clad doctor and replaces the F-049 example with a hash example; src/agents/reviewer.md drops the broken ironclad-design/13 cross-link from the frontmatter description and the guardrails table; src/agents/observability.md adds .cladding/events.log.jsonl to the Sources table and adds a Sentinel-miss summary entry to the Reports list pointing at clad doctor; src/agents/README.md retires the broken ironclad-design/14 cross-link with a self-contained CLAIM and moves CONVENTION_DRIFT and UNTESTED_AC out of [TBD] into a new [DONE_SINCE_T9] section since both detectors landed in src/stages/detectors/; the persistent ironclad-design §1.2 Soft Shell cross-link is removed from all five personas because the inline guidance already names the src/ui/softShell.ts helpers'
    text: The system shall realign every persona canonical prompt under src/agents/ with v0.3.40 reality so a consumer project installing the cladding plugin receives accurate guidance.
    evidence_refs: [src/agents/developer.md, src/agents/planner.md, src/agents/orchestrator.md, src/agents/reviewer.md, src/agents/observability.md, src/agents/README.md]
  - id: AC-002
    ears: ubiquitous
    action: bring the four existing skills/<verb>/SKILL.md surfaces back to parity with the current clad CLI
    response: 'skills/init/SKILL.md grows the --scan, --no-llm, and --roots flag entries, documents the scan artifacts (docs/conventions.md, spec/architecture.yaml, spec/capabilities.yaml, docs/project-context.md, spec/scenarios/README.md), and clarifies that new features use F-<hash6> with a one-liner generator command; skills/run/SKILL.md adds the --cwd <path> flag and a post-drive clad doctor cross-reference; skills/status/SKILL.md disambiguates legacy F-NNN from F-<hash6> in the rows description; skills/check/SKILL.md notes that the Drift stage runs every active detector under src/stages/detectors/ with the current 24/24 count and the build-plugin auto-recount mechanism'
    text: The system shall bring the four existing skills/<verb>/SKILL.md surfaces back to parity with the current clad CLI so adopters do not discover undocumented flags by reading the binary's --help output.
    evidence_refs: [skills/init/SKILL.md, skills/run/SKILL.md, skills/status/SKILL.md, skills/check/SKILL.md]
  - id: AC-003
    ears: ubiquitous
    action: ship a canonical SKILL.md for every verb in the clad CLI so the plugin manifest is no longer missing five entries
    response: 'skills/doctor/SKILL.md ships the v0.3.40 LLM-dispatcher health surface — documents --cwd, --json, the DoctorReport shape, the formatted-text sections (event-type breakdown / phase × cause × fallback / top-missed / recent errors / tuning hint), exit codes, and when to run; skills/checkpoint/SKILL.md ships the Iron Law backbone Phase 1 (iron-law.md §2.5) entry — payload shape (featureId / gitHead / specDigest / timestamp), no working-tree mutation, when-to-use list, pairing with rollback; skills/rollback/SKILL.md ships the partner verb — --reason flag, exit codes, the explicit do-not-checkout policy, when-to-use list, pairing with checkpoint; skills/route/SKILL.md ships the natural-language → verb classifier — read-only no-workspace contract, conservative unknown fallback exits 1; skills/work/SKILL.md documents the current stub behaviour and reserves the verb for the future intent-routing entry point so plugin manifests can carry the placeholder without surprise'
    text: The system shall ship a canonical SKILL.md for every verb in the clad CLI so the plugin manifest is no longer missing five entries.
    evidence_refs: [skills/doctor/SKILL.md, skills/checkpoint/SKILL.md, skills/rollback/SKILL.md, skills/route/SKILL.md]
  - id: AC-004
    ears: ubiquitous
    action: regenerate the auto-mirrored plugin manifests so the agents/ and plugins/codex/skills/ and plugins/gemini-cli/commands/ surfaces match the canonical sources
    response: 'npm run build:plugin auto-detects the new verb count (6 → 11) and copies every src/agents/<persona>.md → agents/<persona>.md + plugins/codex/skills/<persona>/SKILL.md and every skills/<verb>/SKILL.md → plugins/codex/skills/<verb>/SKILL.md + plugins/gemini-cli/commands/<verb>.toml; the plugin manifest detectors count stays at 24/24 because no detector was added; the build is idempotent so reruns are safe in CI'
    text: The system shall regenerate the auto-mirrored plugin manifests so the agents/ and plugins/codex/skills/ and plugins/gemini-cli/commands/ surfaces match the canonical sources without hand edits.
    evidence_refs: [agents/, plugins/codex/skills/, plugins/gemini-cli/commands/]
  - id: AC-005
    ears: state
    condition: while the canonical persona sources reference the inline src/ui/softShell.ts helpers
    action: remove the broken ironclad-design/03-ux-routing.md §1.2 cross-link from every persona because the helper names already convey the guidance
    response: 'all five personas (specialists / librarian / orchestrator / reviewer / observability) drop the trailing "See ironclad-design/03-ux-routing.md §1.2" sentence; the Soft Shell paragraph keeps its substantive guidance (use featureLabel / gateLabel / haltMessage from src/ui/softShell.ts) so no behavioural advice is lost; a grep for "ironclad-design" across src/agents/ and skills/ returns zero results post-edit so a future build cannot silently re-introduce the broken path'
    text: While the canonical persona sources reference the inline src/ui/softShell.ts helpers, the system shall remove the broken ironclad-design/03-ux-routing.md §1.2 cross-link from every persona because the helper names already convey the guidance.
    evidence_refs: [src/agents/]
  - id: AC-006
    ears: ubiquitous
    action: verify the post-edit state through the standard cladding gates so the doc cleanup ships with the same quality bar as a code cycle
    response: 'npm test continues to report 779/779 passing because the createProgram registry stayed at 11 verbs; npm run lint and npm run typecheck remain clean because no source file was touched; npm run build:plugin reports 5 personas + 11 verbs synced; clad sync reports 115 features valid (was 114) with the new F-40327b shard; clad check --strict reports Type / Lint / Drift / Architecture / Secret / Unit tests / Coverage all PASS; manual greps for ironclad-design, docs/code-style, and F-049 across src/agents/ + skills/ return empty results'
    text: The system shall verify the post-edit state through the standard cladding gates so the doc cleanup ships with the same quality bar as a code cycle.
    evidence_refs: [tests/cli/clad.test.ts, scripts/build-plugin.mjs]
