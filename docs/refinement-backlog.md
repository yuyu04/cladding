<!-- Cladding · Tier B · refinement backlog · Refreshed by: manual -->

# Refinement backlog — audited candidates awaiting their own cycle

Survivors of the 2026-07-03 whole-repo audit (baseline: `clad check --strict` 40.5s full / 11.4s pre-commit; session-resident cost ~21.4KB ≈ 5,300 tokens; 2012 tests green). Each candidate below passed an adversarial review in a separate context but was **not** executed in the first refinement pass. Each row carries impact / effort / risk. Items that were reviewed and **rejected** are recorded at the bottom so they are not re-litigated from scratch.

| # | Item | What / conditions discovered by adversarial review | I/E/R |
|---|---|---|---|
| B1 | Deprecate `clad_get_context` | Mark deprecated in 0.9 (tool stays registered); remove only after `clad_get_working_set` adoption is confirmed **per the written rule in docs/b1-adoption-protocol.md** (its gate: verdict `confirmed` over the observation window). Removal must update tests/serve/server.test.ts:798-800, serve-telemetry + session-report tests, docs/glossary.md:87, and reconcile the AC in spec/features/working-set-assembler-06dfdad6.yaml:53-57 ("without altering clad_get_context"). | M/S/M |
| B2 | Shrink the CLAUDE.md `## cladding` template | src/init/host-instructions.ts:56-85 (1,477B). MUST preserve the literal strings `anti-self-cert` (CLADDING_AUTHORED_SIGNATURE) and `Feature cycle — one at a time` (FRESH_MARKER) — dropping either makes every fresh CLAUDE.md self-flag stale (host-instructions.ts:103-115) and `clad update` churns forever. AGENTS.md prose is untouchable (sole policy surface for hookless hosts). Realistic saving re-estimated at ~200-300 tokens, not ~369. | S/S/M |
| B3 | Inline the dead `resolveThreshold()` seams | 4 copies return bare constants: planned-backlog.ts:65, hollow-governance.ts:53, scenario-coverage.ts:42, project-context-drift.ts:43. Premise correction from review: the `max_planned_ahead` knob is NOT documented anywhere user-facing — planned-backlog.ts:61 says "intentionally not wired yet". Delete the indirection (inline constants); do NOT wire an unrequested config knob. | S/S/L |
| B7 | Hook-tier detector profile | PostToolUse currently runs ALL 41 detectors including subprocess ones (madge 1.27s + secretlint 3.75s) ≈ ~6s per significant edit (hook.ts:1003). Proposal: interactive hooks run in-process detectors only (~1s); subprocess detectors stay in Stop/commit tiers (Stop already re-checks strict at turn end). Risk: loses instant post-edit circular-import/secret feedback. Needs its own adversarial review before implementation. | H/M/M |
| B8 | Loop-engineering positioning section | One README section: using cladding as the agent loop's verifier/state layer (host owns the loop; `clad check --json` as the feedback signal; `clad done` flip-gate-revert as the honest stop condition; events.log as loop memory). No code change. | M/S/L |
| B9 | Spec-bind the 5 unbound A/B case studies | docs/ab-evaluation/case-{doverunner-scale,efficiency-measurement,graph-efficiency,iterative-vs-fixed-vapt,working-set-landmine}.md — deletion was REJECTED (README.md:117 "4× less" is receipted only by case-efficiency-measurement.md:23; deleting the four NULL-result files = publication bias). Instead bind them to F-16138071 / F-06dfdad6 / infer-deps features so the knowledge graph stops treating them as orphans. | S/S/L |
| B10 | spec-conformance stage npx cold-boot | stage_2.3 boots a third vitest via `npx` (spec-conformance.ts:75). Dormant on this repo (stage skips without oracle tests); only matters for adopters with oracle policies. Optimize the spawn (direct bin resolution) when it first shows up in an adopter profile. | S/S/L |
| B11 | Complete the 0.6.0 verb-rename residue (refine→clarify, drive→run, panel→status) | Surfaced by the alias removal (F-d25041ac): provenance banners still say `clad init / clad refine` (src/cli/init.ts, scan/*, spec/new.ts, clarify.ts), init.ts:415/422/428 user-facing remediation messages recommend the removed verb, detector messages in project-context-drift/spec-conformance/fixture-reference name it, and the banner strings are EXACT-MATCH test-pinned (tests/scenarios/ab/_drift-injection.ts:64, ab-extended/_curator.ts:1038/1054/1087) — a coherent ~15-src + ~8-fixture migration, deliberately not smuggled into the 2-module alias-removal feature. Highest-severity slice — the init.ts remediation messages — was extracted and fixed pre-release (F-fe0f7a96); remaining scope = banners + fixtures + 3 stale internal comments (init.ts:390, scan/intent-onboarding.ts:696, scan/onboarding-state.ts:7). | M/M/M |

## Reviewed and REJECTED — do not re-open without new evidence

- **Merge unit (2.1) + coverage (2.2) into one vitest run**: conformance/fixtures.yaml:82-85 pins "tests green + coverage below floor" as distinct stage outcomes; `mvn jacoco:report` runs no tests on JVM; gate-golden-matrix.test.ts pins stage independence. The double suite run is the deliberate price of separable verdicts.
- **Merge `clad_run_check` into `clad_run_gate`**: deliberately separate latency surfaces (cheap agent-loop drift view vs full pipeline); blast radius (doctor-hosts.ts:162, F-073 AC, SECURITY.md:26, glossary, dogfood receipts) dwarfs the ~300-token saving.
- **Delete scripts/migrate-dogfood-v0.3.16.mjs**: it is modules[0] + an evidence_ref of done feature spec/features/dogfood-recovery-v0-3-16-245bd5.yaml — deletion turns MISSING_IMPLEMENTATION red on cladding's own gate. (The gate defending its own audit trail is the concept working as designed.)
- **Shared file-tree walk across the 6 walking detectors**: <1s headroom; divergent roots/filters (mainRoot on JVM, per-detector ignore-sets) make a shared walk leaky and possibly slower.
- **Delete the 5 A/B case studies / 4 benchmark reports**: deletion stays REJECTED — benchmarks/ is self-declared append-only audit history (docs/README.md:23), the 5 A/B case studies are publication-bias-protected NULL results bound under B9, and live CHANGELOG links (the 0.4.0, 0.6.0, and event-store-cell entries) must keep resolving. **Narrowed 2026-07-05 (new evidence, all-shard cross-check — F-27e56a00):** the append-only policy now permits *compression* of a superseded run-log to a dated abstract at its original path (docs/README.md:23 carve-out), so history stays reachable and negative results stay published while the live docs shed thousands of superseded words. Two qualified and were compressed in place: `v0.4.0-consistency-bench.md` (no live claim cites its numbers — the README efficiency receipt is `docs/ab-evaluation/case-efficiency-measurement.md`, and the CHANGELOG refs are history) and `v0.6.0-real-user-verification.md` (operationally superseded by the 0.7.1 real-usage battery + 0.8.0 surface-fired telemetry — its conformance NULLs, 3 shipped-bug catches, and oracle-policy design-defect finding are preserved verbatim in the abstract, so compression, not deletion). REMAIN byte-identical and protected: the event-store pair (F-066 done-feature modules + live GOVERNANCE.md:120 citation), `cross-vendor-spec-convergence.md` (publication-bias-protected positive result), and `prereg-context-hypothesis.md` (un-discharged pre-registration — its 3-arm experiment was never run and the kill-criterion was never adjudicated). Do not delete any benchmark; do not re-open compression of the protected four without new evidence.
- **SVG count auto-verification**: cycle.svg says "40 checks" (different noun), titles split across tspan nodes, ×2 locales — regex matching is brittle; counts are repaired manually and guarded in markdown prose only.
- **Detector merges/demotions (all 41 kept)**: MISSING_TESTS → UNTESTED_AC → UNVERIFIED_AC is a designed disjoint-trigger ladder; STATUS_DRIFT uniquely owns hollow-completion; dormant detectors (PERFORMANCE_DRIFT, STALE/EVIDENCE_MISMATCH, HOST_CLAIM_DRIFT) cost nothing when preconditions are absent and are real for adopters.
- **registerDetector/clearDetectors registry & 1-entry SDK_REGISTRY**: documented extension seam + test affordance; multi-provider roadmap reserves the SDK slots.
- **SCALE_GATE=8 constant dedup (4 copies)**: trivial churn, no behavior change.
- **Committed who-ledger (attestation ledger lines carrying identity + timestamp) — deferred, not rejected**: what it would be — the per-module attestation (spec/attestation.yaml, v2) gains committed ledger lines carrying author identity + timestamp, so who/when survive in tracked content instead of only in the local `.cladding/events.log.jsonl` (gitignored, single 5MB generation → history beyond it is destroyed; records the git/OS account only, AI-vs-human indistinguishable). Deferred because no external team has demanded audit evidence yet — shipping it now is speculative machinery, so the who/what/why README claim was corrected to its verified level instead (F-3c2bf8b9). Reopen trigger: a real external team requires audit evidence of who/when.
- **Independence has no first-party surface — deferred, not rejected**: the `independent | self-certified` label ships and is judged correctly (`src/hitl/independence.ts`), but nothing in the product lets a user reach it. Three findings, one disease. (a) **The policy switch is read-only from cladding's side** — `independence_policy` appears in `spec/types.ts`, `spec/schema.json`, and one consumer (`src/cli/clad.ts:856`); no CLI verb, MCP tool, or onboarding path ever writes it, so turning enforcement on means hand-editing `spec.yaml`, the one kind of work this product otherwise never asks of a user (the 2026-07-24 E2E had to "hand-set" it — `docs/dogfood/e2e-role-contract-2026-07-24.md:51`). No persona brief, `AGENTS.md`, or `CLAUDE.md` names the key either, so a host AI cannot act on "make completion stricter" without being handed the literal; README.md's Multi-Agent section is currently the setting's only user-facing documentation, which is why the key stays spelled out there. (b) **G1 — `independent` is MCP-gated**: the only first-party writer of independence-eligible evidence is `clad_author_oracle`, so a CLI-only project can perform the separation and still never record it, and under `independence_policy: require` is hard-blocked with no first-party exit. (c) **G2 — `human`-authored evidence has zero writers anywhere**: `recordOracle` hard-codes `identity.author: 'llm'` (`src/oracle/record.ts:105`) and the other two `appendEvidence` call sites write `tool`/adapter identities, so the human half of the label's disjunction — and the `checkAc` demand at stage_4.1, which predates the label — is satisfiable only by hand-editing `.cladding/audit.log.jsonl`. Deferred because no team has asked to enforce independence yet; a CLI attest surface plus a brief mention would be speculative machinery until then, so the README discloses both limits in plain words instead (F-3fd220d8). Reopen trigger: a team tries to turn `independence_policy: require` on, or requires evidence of a person's sign-off.
